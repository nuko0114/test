using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScript : MonoBehaviour
{
    public string name = "nuko";

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(name);
        Debug.Log("ba-ka");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
